# Github pages should manage all the dependencies.
source "https://rubygems.org" do
    gem "github-pages", "~> 206", group: :jekyll_plugins
end

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }